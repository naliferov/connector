import Exec from "../../io/exec/Exec";

export default class ProcessManager {

    exec: Exec;

    constructor(exec: Exec) {
        this.exec = exec;
    }

    startProcess(cmd, ) {

    }

    stopProcess() {

    }
}